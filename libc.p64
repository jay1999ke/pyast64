def main():
   num = 0
   puts('Enter a number: ')
   scanf('%d', adr(num))
   printf(r'hello world (%d)!\n', num)

   fp = fopen('myfile', 'w')
   fprintf(fp, r'hello world (%d)!\n', num)
   fclose(fp)
